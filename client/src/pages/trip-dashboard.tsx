import * as React from 'react';
import { AppBarComponent } from '../components/appbar';

export class TripDashboard extends React.Component<any, any> {
  render() {
    return (
      <div>
        <AppBarComponent />
        React trip planner from trip dashboard
      </div>
    );
  }
}
